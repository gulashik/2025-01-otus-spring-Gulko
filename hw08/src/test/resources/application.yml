spring:
  data:
    mongodb:
      database: test_database
      mongo-db:
        write-concern:
          journal: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2

mongock:
  change-logs-scan-package:
    - ru.otus.hw.objects.mongock.changelog
  mongo-db:
    write-concern:
      journal: false # todo нужно для тестов
    read-concern: local